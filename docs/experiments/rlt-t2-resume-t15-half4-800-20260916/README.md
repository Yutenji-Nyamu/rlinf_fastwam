# RLT四采样：τ2 fresh800与τ1.5续至累计800

用户明确要求GPU7结束τ1/成功倍率2，改为τ2/倍率1、每轮4采样、训练800轮；GPU6结束当前τ1.5/倍率1进程，从完整CP425继续至累计800轮。先准备配置与恢复验证，逐卡停止后立即启动，再整理旧实验产物。

| GPU | DVAC温度（内/外） | 成功BC倍率 | 起点 | 总终点 |
| --- | --- | --- | --- | --- |
| 6 | 1.5 / 1.5 | 1 | 原运行完整CP425，第一条新采集日志R426 | 累计R800，即额外375轮 |
| 7 | 2 / 2 | 1 | 原current-AR Stage1 CP2000，空Stage2训练/回放状态，第一轮R1 | Fresh R800 |

两组继承成功Clean4预算：每轮4环境、rollout_epoch1，B512/micro256、UTD5、critic:actor=2:1；actor/critic LR均1e-4且constant；10k池门槛、15k critic初始化、每轮最多800次更新、回放容量80k；BC/Q课程10k+25k，7/.05→2.5/.45；固定20次评估和保存均每25轮。温度映射为exp_mean，双α1，成功BC总倍率1；没有倍率或方向切换课程。本轮总轮数由600增加至800，是明确授权的预算变化。

代码逐文件继承703408b1（算法实现来源7c9372c6），无算法源码变更。`config-review.json`保留相对Clean4及当前τ1.5运行的全部实配差异；本提交仅增加可核验的运行证据。

GPU6是常规训练恢复，包含模型、优化器、学习率调度、target-Q、回放与RLT trainer状态；恢复验证运行生产继承方法检查原配置与800终点的契约，核对DCP元数据、回放索引并读取少量样本。它不是环境或回放采样RNG逐位完全重放。`resume-validation-v2.json`保存通过的恢复检查；初版检查器漏提取继承方法的问题只在运维验证脚本修正，没有改训练代码。

两个`STARTUP_VERIFIED.json`必须通过才允许推送。GPU6检查恢复日志、R426首轮、成熟更新计数与池规模及实际critic更新；GPU7检查R1首轮、空Stage2的预收集状态。源码SHA、实际配置、12个actor及保护进程身份均核对。启动验收证明配置和恢复流程正常，不预判τ2的效果。

旧GPU6包保留旧run全部已记录历史；CP425之后可能仍有未进入该checkpoint的尾部日志。后续绘制续训主曲线应取旧R1–425加新R426起，不把旧CP425之后尾部重复拼入。旧GPU7与新τ2为独立实验，曲线分开。旧两组的events、指标与五种图另在旧分支收尾包留存；模型和回放保留服务器。

本目录包含精确配置、命令、已审查无凭据的环境设置、源码身份、恢复和切换回执。`runner.max_steps=800`指训练轮总终点，不是Adam更新次数。正常停止条件为达到R800、用户主动停止或不可恢复错误；没有性能阈值自动停训。
