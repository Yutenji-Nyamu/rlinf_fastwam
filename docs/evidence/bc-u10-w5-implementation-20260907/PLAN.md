# BC8/U10 + DVAC[0,5]实施与切换账本

## 授权和合同

用户授权：实现、针对性测试、推送、不smoke；旧GPU6/7两项停止、轻量ZIP/曲线/分支归档，然后从相同π0.5 SFT/空成功池启动两项正式100轮。保持8次尝试/轮、micro32/global1024；U2→U10。GPU4/5 GRPO和shared Ray/他人不动。

共用新增 `algorithm.online_bc.max_success_chunks: null`：默认关闭；设3只接纳成功query数<=3的完整episode，拒绝整条4chunk成功，不截断成假成功；本次正式仍null，符合用户“跑过一两次后设3”。只作用新入池数据，环境采集/成功率和DVAC全新query标尺不变；旧replay不追溯清除。

DVAC新增显式映射选择与上下界，旧centered模式保持默认兼容；本次选择无chunk均值归一的有界分段线性：z=-2,-1,0,1,2→w=0,0.5,1,3,5。保留log/history5/zclip2、首轮w1、入池固定、原FM/mask/分母。借GRPO已有非局部有界映射的设计，不导入GRPO目标/统计对象。

后续按预检实际resolved逐叶继承，记录输出盘、完整命令/预算/资源。各副作用操作唯一attempt/receipt，不自动重放失败操作。训练正式启动只检查启动健康即回来，不等待首次评估/长期监控。

## 进度

- 已读短入口、上一轮证据；本轮新预检/源码/测试/发布/停止/归档/启动待执行。
