# Method-only OmegaConf merge overlay, not a standalone launch configuration.
# Merge into the inherited SARM resolved config; retain 4/U5, kappa=2, length off.
algorithm:
  online_bc:
    rabc:
      clean_mix: 0.5
