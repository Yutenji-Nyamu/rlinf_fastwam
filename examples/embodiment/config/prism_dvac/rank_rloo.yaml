# @package _global_
# Method-only overlay. Inherit the actual pi0.5 Control runtime and resource budget.
algorithm:
  adv_type: prism_rloo
  normalize_advantages: false
  filter_rewards: false
  dvac_gradient_weighting:
    mode: "off"
  prism_dvac:
    enabled: true
    selected_l: 3
    quality_lambda: 0.2
    log_eps: 1.0e-12
