defaults:
  - robotwin_adjust_bottle_rlt_stage2_ac_mlp_8env250_teacher_dvac_w0to2
  - _self_

# User-approved single-process fresh Stage 2 run to absolute cycle 480.
# All RLT and teacher-DVAC algorithm parameters remain inherited from the
# successful 8-env formal protocol and the validated [0,2] smoke.
runner:
  logger:
    experiment_name: "robotwin_adjust_bottle_rlt_teacher_dvac_w0to2_formal_fresh480_v1"
  max_steps: 480
  val_check_interval: 25
  save_interval: 25
  resume_dir: null
