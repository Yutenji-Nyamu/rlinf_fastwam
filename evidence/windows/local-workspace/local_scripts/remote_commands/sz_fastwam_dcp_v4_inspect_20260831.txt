RUN=/data/chenyiteng/results/rlinf-shenzhen/fastwam-grpo/runs/fastwam-grpo-smoke1-current-2gpu32x1-g8-b256-u1-m10-phys23-dcp-v4-roundtrip; RELOAD=${RUN}-reloadcheck; PACKET=/data/chenyiteng/results/rlinf-shenzhen/fastwam-grpo/packets/fastwam-grpo-smoke1-current-2gpu32x1-g8-b256-u1-m10-phys23-dcp-v4-roundtrip; echo '--- processes'; ps -eo pid,ppid,etimes,stat,args | grep -F "$PACKET" | grep -v grep || true; echo '--- files'; find "$RUN" "$RELOAD" "$PACKET" -maxdepth 4 -type f -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' 2>/dev/null | sort | tail -40; echo '--- worker tail'; test -f "$PACKET/worker.log" && tail -80 "$PACKET/worker.log" || true; echo '--- driver tails'; test -f "$RUN/runtime/driver.log" && tail -40 "$RUN/runtime/driver.log" || true; test -f "$RELOAD/runtime/driver.log" && tail -40 "$RELOAD/runtime/driver.log" || true; echo '--- gpu'; nvidia-smi -i 2,3 --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader,nounits
