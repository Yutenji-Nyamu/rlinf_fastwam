# 2026-09-06 轻量Git归档、精确大文件清理与上下文裁剪账本

## 授权与边界

用户本轮要求：本人历史实验除视频/大文件外推Git；smoke大权重可全部清；Sidney π0.5 GRPO、DSRL、online-BC的非最后checkpoint大文件清理；大幅裁剪上下文入口，保留习惯；深入核对BC/DVAC差异、讨论U和权重范围。

执行解释：覆盖本地rl研究资料与深圳本人实验结果，不覆盖其他用户或无关个人目录。归档前扫描凭据；大文件/视频排除并记录。清理只删除明确checkpoint/测试权重中大于1GiB的常规文件，保留目录、日志、配置、小文件和各正式run最后完整代；活动写入/打开文件、未解决恢复依赖跳过。旧smoke若被正式起点引用则保护。当前训练和共享Ray不改，U/权重范围/RNG修复本轮仅诊断讨论。

## 操作记录（边做边补）

1. 完整入口/专题读取、恢复上一轮证据；发现根Git无提交、无remote，不能假装本地docs已在某个远端。拟使用既有个人RLinf仓库的独立归档分支，不碰运行分支代码；目的地与网络/权限先核对。
2. OpenAI Docs核对AGENTS加载规则；先无损归档现入口再裁剪。归档不等于删除历史研究。
3. 预备现场刷新与BC/DVAC完整diff读取；未删除或push。
4. 原AGENTS/PROJECT_CONTEXT/HANDOFF逐字复制并校验SHA256归档；新入口合计6846字节，取消强制读取旧窗口全文与无变化的重复读取。保留用户习惯、安全授权和专题路由；不删研究材料。
5. 10:53现场BC48、DVAC46、GRPO158；无新fatal/OOM。全量结果清单210033个小文件候选，其中约20万replay；用户明确回复按数据类别总量排除replay和类似大体量数据，不进Git，保留清单。
6. 完整配置只有17项预期差异，源码10文件差异中生产6文件；CPU回归28/28通过。真实记录读取脚本误把多元素policy_version当标量，修改审计脚本为逐元素归集；没有修改训练代码。
7. 准备106个精确大checkpoint文件、879.21GiB的候选清单，正式latest必要文件逐项核验。执行器会再次核对路径/inode/mtime/owner/open-fd/活动续训引用；若Step100仍被活动命令引用则跳过，不以候选量冒充实际删除量。未删除目录。
8. 11:15第一批完成100文件825.5119GiB。保守匹配把Step10也当作Step100引用而跳过；修正为路径边界匹配，单独列3文件补充批。首份补充JSON经JS序列化丢纳秒精度，stat保护拒绝（零删除）；改用保留Int64精度的清单，11:23完成3文件26.8484GiB。合计103文件852.3603GiB；活动续训命令仍引用Step100的3文件保护。正式latest原23个必要/附属文件前后stat全通过。无目录删除，已删大权重不可由Git恢复。
9. 真实首轮BC16成功/55chunk；DVAC9成功/29chunk且权重严格全1。后续真实DVAC权重正常非单位且逐chunk均值1。完整结论见BC专题evidence/PI05_BC_DVAC_FULL_DIFFERENCE_AUDIT_20260906.md。
10. 开始本地逐类过滤与凭据脱敏打包；不把数十万个replay、视频、模型放入Git。服务器归档使用独立artifact-only分支，当前运行源码和现有dirty不改。
11. 服务器独立分支建树成功，收录55939项；原worktree和dirty文件保持原样。Windows初次遍历后，停止自己本地旧打包助手（只处理生成的临时档案，没有停止服务器进程），再清除生成副本中的1484个opaque Git对象，重新审查。最终49130文件+审查报告，608565757字节传输包，已知凭据残留0。
12. 两项启动锁到当前HEAD的rlinf/examples/tests差异均为空。11:30刷新三项运行、无新fatal/OOM；BC已有完整Step50。全量归档包已传到本人维护目录，正在导入独立归档树和commit，尚不以commit冒充push。
13. 主归档commit 15c96b50e3c8333b99ba04f30471b103559f4bcb完成，105073项；新分支clean，完整树大文件/已知凭据/opaque对象复核均0。追加最后文档/脚本delta后只push该新分支，无force；最终以远端SHA核验为准。

## 待完成

- 现场清理候选/保留点/open-fd/恢复引用核验、执行与磁盘复核。
- 轻量结果全量清单、凭据过滤、独立归档提交与远端核验。
- 本地入口无损压缩、链接/尺寸验证。
- BC/DVAC配置逐叶/源码/首轮与累计池随机路径审计；不靠加U/扩大范围掩盖未排除的问题。
