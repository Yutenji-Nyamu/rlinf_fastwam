# 09-05 经授权的旧checkpoint大文件清理账本

## 授权与精确范围

用户本轮明确批准前轮81个旧GRPO/PPO大文件，以及两个BC组件探针、BC v7/DVAC smoke各第1代、旧π0.5/Fast-WAM非最后代；要求只清理看准的上GiB文件、不删目录。以19:36讨论manifest和4个探针绝对路径为候选，不扩展到新v8、当前Sidney、DSRL/RLT、模型/环境/Ray/他人数据。预计107文件、约584.61GiB，实际以执行前复核为准。

本轮π0.5在线BC只研究和规划，不修改生产代码或启动新训练。此前“禁止清理”已由本轮针对性授权覆盖，其他限制不变。

## 逐操作记录

1. 读取根规则/当前交接/BC专题、上轮清理讨论manifest与9月1日执行账本；当前授权与旧授权分开。
2. 启动只读全机刷新，固定host key、普通账号密码认证，不动共享Ray/其他用户。
3. 准备精确候选及preflight：复核绝对路径/大小/属主/单硬链接/无软链接，保留点文件与DCP索引完整性，活动进程和配置恢复路径，打开文件；检查仅元数据/ZIP索引，不加载大模型或跑GPU。
4. 删除动作尚未执行。只有读回preflight并确认保留点/依赖后，才逐文件unlink；执行时重新核对inode/size/mtime和保护文件，并同步持久化每条结果。不使用通配符或递归删目录。
5. 20:36第一次只读preflight在GRPO global-z matched4GPU v4的最大Step30发现DCP索引引用缺失分片；任何unlink尚未执行。不能把历史最大编号当最后完整。窄改清理工具：一次枚举所有保留点，按root延后不完整者，其余已验证子集可执行，不降低保留点验证；完整缺失列表记录在新preflight，不猜跨run lineage。
6. 服务器PATH无rg，一次只读辅助扫描因此退出；已改用标准目录元数据遍历/小配置读取，未安装工具。20:41:38—20:41:42重新preflight通过101文件570.0803GiB，19个保留点95文件、淘汰代121个小文件已记录；无目标打开文件/活动恢复引用，所查历史小配置亦无命中。另一条旧π0.5 smoke第2代早在9月1日已只剩full model，明确按已有model-only保留，不冒称训练可恢复。
7. 暂缓6文件14.5250GiB：GRPO global-z matched4GPU v4的Step10/20残留，其最大Step30的DCP不完整；本轮先不动该run任何文件。将对已验证101文件执行精确unlink，再独立连接复核，不自动扩大到暂缓项。
8. 首次execute在SSH发送长命令请求阶段EOF，未收到远程执行结果，未盲目重放。20:46独立只读连接检查专属audit目录不存在（所有unlink之前会先独占创建该目录），证明没有进入删除循环。清单352KB，不再内嵌SSH命令，改走原连接的stdin传JSON；不换认证路线。后续仍完整重查所有文件元数据/保护点后才允许执行，任何缺失即停。
9. 20:49:26—20:49:39第二次execute完成，session2908最终exit0：精确unlink 101文件，allocated总570.080299GiB；没有递归删除、移动目录或扩大候选。执行前后95个保留文件和121个小文件元数据一致，所有删除目标已不存在。远程每项删除均写入独占audit目录的deletions.jsonl并flush/fsync，完整result.json已回传。本地控制器发现已有结果时禁止重放。
10. 上下文中断后先poll原session2908取得成功结果，未重跑execute。21:12:17独立SSH只读verify通过：101目标不存在、95保留文件和121小文件不变。保存[执行结果](SZ_APPROVED_CHECKPOINT_PRUNE_RESULT_20260905.json)和[独立复查](SZ_APPROVED_CHECKPOINT_PRUNE_VERIFY_20260905.json)。

## 最终结果与保留边界

| 已删除类别 | 文件数 | allocated GiB |
|---|---:|---:|
| 旧GRPO较早代 | 57 | 243.77 |
| 旧PPO较早代 | 18 | 89.43 |
| 两个BC组件探针 | 4 | 33.40 |
| BC v7／DVAC smoke第1代 | 4 | 34.38 |
| 旧π0.5非最后代 | 16 | 142.18 |
| 旧Fast-WAM非最后代 | 2 | 26.92 |
| 合计 | **101** | **570.08** |

原81个GRPO/PPO候选中实际清75个、333.20GiB，另6个14.525GiB因第7项原因暂缓。两条smoke的Step2保留；新BC v8两代完全不在本次范围。旧π0.5 smoke的末代本来就是model-only，不代表完整续训点；其余保留点只验证文件结构、索引/ZIP与字节范围，未做加载后训练测试。

执行前/data普通用户可用343.16GiB，执行后913.24GiB；21:12全机刷新/data可用913.22GiB（73%已用）、/home可用1246.15GiB（47%已用）。期间当前Sidney保存了Step120，不能拿19:36/20:27磁盘值直接相减来计算本次删除量。

只删除明确列出的≥1GiB权重/训练分片；目录、日志、曲线、配置、metadata、replay和learner小文件保留。没有处理当前Sidney、DSRL/RLT、其他用户、模型资产或共享Ray。删除不是回收站操作，未创建权重备份，不能靠日志恢复这些历史模型。

远程逐文件审计：`/data/chenyiteng/results/rlinf-shenzhen/checkpoint-cleanup-20260905-approved/{preflight.json,deletions.jsonl,result.json}`。后续如处理暂缓6文件，必须先核实这条run的可用恢复点/跨run依赖，不能只按目录最大编号继续删。
