 docs/online_bc.md                                  | 14 ++++--
 .../robotwin_adjust_bottle_online_bc_openpi.yaml   |  4 +-
 .../embodiment/openpi/openpi_action_model.py       | 14 +++---
 tests/unit_tests/test_online_bc.py                 | 54 ++++++++++++++++++++--
 4 files changed, 69 insertions(+), 17 deletions(-)
diff --git a/examples/embodiment/config/robotwin_adjust_bottle_online_bc_openpi.yaml b/examples/embodiment/config/robotwin_adjust_bottle_online_bc_openpi.yaml
index b7f77395..8e707049 100644
--- a/examples/embodiment/config/robotwin_adjust_bottle_online_bc_openpi.yaml
+++ b/examples/embodiment/config/robotwin_adjust_bottle_online_bc_openpi.yaml
@@ -115,7 +115,8 @@ actor:
   enable_offload: true
   enable_drq: false
   model:
-    precision: bf16
+    # Preserve native OpenPI BF16 backbone / FP32 projections, as in DAgger.
+    precision: null
     model_path: ${oc.env:PI0_MODEL_PATH}
     num_action_chunks: 50
     # num_steps inherits model/pi0.yaml (4), as in official pi0 DAgger/GRPO.
@@ -125,6 +126,7 @@ actor:
       config_name: pi0_aloha_robotwin
       num_images_in_input: 3
       train_expert_only: true
+      image_augmentation: false  # User-selected BC baseline; retain resize/norm.
       action_chunk: ${actor.model.num_action_chunks}
       action_env_dim: ${actor.model.action_dim}
   optim:
diff --git a/rlinf/models/embodiment/openpi/openpi_action_model.py b/rlinf/models/embodiment/openpi/openpi_action_model.py
index dc2ae76c..058009b2 100644
--- a/rlinf/models/embodiment/openpi/openpi_action_model.py
+++ b/rlinf/models/embodiment/openpi/openpi_action_model.py
@@ -16,7 +16,7 @@ import math
 import random
 from collections.abc import Sequence
 from concurrent.futures import ThreadPoolExecutor
-from dataclasses import dataclass, field, replace
+from dataclasses import dataclass, field
 from typing import Any, Literal
 
 import numpy as np
@@ -73,6 +73,7 @@ class OpenPi0Config(Pi0Config):
     rtc_guidance_clip: float = 5.0
     # training config
     train_expert_only: bool = False
+    image_augmentation: bool = True
     safe_get_logprob: bool = False
     joint_logprob: bool = False  # designed for flow-noise
     double_layer: bool = False  # designed for flow-sde without acceleration
@@ -374,6 +375,11 @@ class OpenPi0ForRLActionPrediction(PI0Pytorch, BasePolicy):
         else:
             raise NotImplementedError
 
+    def _preprocess_observation(self, observation, *, train=True):
+        return super()._preprocess_observation(
+            observation, train=train and self.config.image_augmentation
+        )
+
     def sft_forward(self, data, use_action_chunk_loss: bool = False, **kwargs):
         if hasattr(self, "gradient_checkpointing_disable"):
             self.gradient_checkpointing_disable()
@@ -394,12 +400,6 @@ class OpenPi0ForRLActionPrediction(PI0Pytorch, BasePolicy):
             ),
             observation,
         )
-        # Replay/FSDP may cast images to BF16. Native training augmentation
-        # builds an FP32 rotation grid, so restore images after the FSDP
-        # boundary; this does not change rollout rendering or model precision.
-        observation = replace(
-            observation, images={k: v.float() for k, v in observation.images.items()}
-        )
         if not isinstance(actions, torch.Tensor):
             actions = torch.as_tensor(actions, device=device)
         else:
[codex/sz-pi0-online-bc 9876c28d] Preserve native OpenPI SFT precision and disable BC image augmentation explicitly
 4 files changed, 69 insertions(+), 17 deletions(-)
VERIFIED_HEAD=9876c28de25bba429d10e2c8b4c85fb19b24c387
