# SZ-H100 两账号现场刷新流水账（2026-08-21）

## 目标与边界

- 账号：`chenyiteng`（日常项目）、`toom`（管理员能力核验）。
- 目标：刷新身份、sudo、网络、磁盘/挂载、GPU/驱动、RAM、进程、EGL/Vulkan、已有项目目录与共享服务器约束。
- 本阶段先只读；不会停止进程、改权限、安装软件、创建项目目录或下载文件。
- SSH 密码只存在于当前进程，不写入本账；固定核对既有主机指纹，认证失败或指纹变化立即停止。

## 操作记录

> 以下从首次 SSH 身份探针开始按实际执行顺序追加。

### SZ-AUD-001 — `chenyiteng` 固定主机指纹身份探针

- 时间：2026-08-21 16:44 CST
- 本地工作目录：`C:\Users\86136\Documents\rl`
- 连接：`chenyiteng@120.241.223.9:22`；期望 host key `SHA256:qw92OXne52y6NsQMQ6+PdjOu0Hosc4ZX78XmkYxcuEY`
- 远端命令：

```bash
hostname; pwd; id -u; id -un; id -Gn
```

- 结果：退出码 0；主机 `admin`，目录 `/home/chenyiteng`，UID 1003，组为 `chenyiteng sudo labdata`。
- 变化：相对 2026-08-19 快照，`chenyiteng` 已新增 `sudo` 组；仅凭组成员不能推断免密或具体 sudo 权限，下一步单独核对。

### SZ-AUD-002 — `toom` 固定主机指纹身份探针

- 时间：2026-08-21 16:45 CST
- 本地工作目录：`C:\Users\86136\Documents\rl`
- 连接：`toom@120.241.223.9:22`；使用同一固定 host key。
- 远端命令：

```bash
hostname; pwd; id -u; id -un; id -Gn
```

- 结果：退出码 0；主机 `admin`，目录 `/home/toom`，UID 1000，组为 `toom adm cdrom sudo dip plugdev lxd labdata`。
- 问题与处理：无。两个账号均完成认证，主机指纹未变化。

### SZ-AUD-003 — `chenyiteng` 机器、存储、GPU、图形栈与网络全量只读刷新

- 时间：2026-08-21 16:48 CST
- 远端工作目录：`/home/chenyiteng`
- 完整 command file：`local_scripts/remote_commands/shenzhen_live_audit_chenyiteng_20260821.sh`
- SHA256：`0a377a29c170562f4f3afdd72c6bb60ec25bb9093e6f2fe635963cf9c5da30b2`
- 执行方式：固定 host-key 的 Paramiko `exec_command`；退出码 0。command file 中的各探针按顺序执行，个别不存在路径/工具允许非零并继续。
- 关键结果：
  - Ubuntu 22.04.5、kernel 5.15、128 CPU、约 2.0 TiB RAM；available 约 1.9 TiB，swap 未使用。
  - `/home` 已扩容到约 2.3 TiB、普通可用约 2.3 TiB；`/data` 普通可用约 3.2 TiB。
  - **`/scratch` 已不存在**；此前计划中的所有 `/scratch/chenyiteng/...` 路径已失效。`/home/chenyiteng/scratch` 也不存在。
  - `/data/chenyiteng` 仍为本人私有，`/data/shared` 为 `labdata` 组协作目录；本次未读取其他用户私有目录。
  - 8 × NVIDIA H100 80GB HBM3 全部 0 MiB/0% compute 使用，无 GPU compute process；driver `575.57.08`，NVLink 拓扑完整。
  - `/usr/local/cuda -> /usr/local/cuda-12.9`，但 `nvcc` 不在当前 PATH；需区分 toolkit 已安装与 shell 环境未暴露。
  - NVIDIA EGL/GLX 库与 `/usr/share/glvnd/egl_vendor.d/10_nvidia.json` 存在；`vulkaninfo --summary` 成功枚举 8 张 H100，另有 llvmpipe。`eglinfo` 不存在。
  - Docker/containerd active，但 `chenyiteng` 不在 docker 组，socket 为 `root:docker 0660`。
  - 系统有 Git/Git-LFS/CMake/GCC/FFmpeg/Vulkan tools；当前普通 shell 没有 `uv`、Conda、pip3，`ninja` 命令也未发现。
  - GitHub 与 PyPI 探针成功；直连 `huggingface.co` 小文件超时，`hf-mirror.com` 成功。
  - 当前非登录 Paramiko shell没有 proxy 环境变量；后续需用登录 shell复核新系统 Mihomo 配置。
  - 当前 `ulimit -n` soft 值为 1024，可能不足以支撑大量 Ray/EnvWorker；先记录，不建立经验硬 gate。
  - 机器上有其他用户 Codex、watchdog 和模型下载进程，但无 GPU 作业；本次未干预。
- source refresh：RoboTwin official `main/HEAD=30954692d06ba7e89f7a6b76064f4062c488fa81`；RLinf official `main/HEAD=7d07a4212ee6858cc333e1d4fab7a37256d1f839`。两者均已前移，旧计划锁不能直接使用。

### SZ-AUD-004 — `chenyiteng` sudo 权限核验与探针问题闭合

- 首次命令：远端 PTY 中执行 `sudo -l`。输入用户密码后 channel 未返回；本地仅有的该次 Python SSH helper PID 被精确终止，连接随即关闭，未执行任何管理写操作。
- 原因：PTY/pager/PAM 返回路径不能提供有界、可审计的结束语义；没有据此判定 sudo 失败。
- 窄修复：新增无凭据 helper `local_scripts/verified_ssh_sudo_probe.py`，SHA256 `769b89c7bd321218f7fcde39480556d3e09c8e4544a4092991cfb97f7a9679e7`；通过无 PTY stdin 把当前进程密码交给：

```bash
env PAGER=cat SUDO_PAGER=cat sudo -S -k -p '' -l
```

- 复测：退出码 0；`chenyiteng` 可执行 `(ALL : ALL) ALL`，但需要本人的 sudo 密码，并非 NOPASSWD。

### SZ-AUD-005 — `toom` 管理视角只读刷新与 sudo 核验

- 时间：2026-08-21 16:53 CST
- command file：`local_scripts/remote_commands/shenzhen_live_audit_toom_20260821.sh`
- SHA256：`9b3fef47c943680c6bd32a78d733c93fe4c75f28e0d8f91247e888ad2f8b92d5`
- 结果：退出码 0；确认 `/scratch` 对管理员账号同样不存在，`chenyiteng` 已列入 sudo 组；docker 组无普通成员，Docker socket 未开放给项目用户。
- 使用同一无 PTY sudo helper 复核：`toom` 也可执行 `(ALL : ALL) ALL`，需要本人密码。
- 权限结论：日常项目继续使用 `chenyiteng` 自身属主；系统包等精确管理员命令可由 `chenyiteng` 临时 sudo，不再需要切到 `toom`。`toom` 仍保留为独立管理员恢复入口。

### SZ-AUD-006 — 新存储布局与系统代理现场复核

- 时间：2026-08-21 17:06 CST
- 远端账号/目录：`chenyiteng` / `/home/chenyiteng`
- 完整 command file：`local_scripts/remote_commands/shenzhen_proxy_storage_refresh_20260821.sh`
- SHA256：`270c5868492b1eb0140331926a6ba53059f329ed8d2ec809aea32cc3dbbdf440`
- 执行方式：固定 host-key 的 Paramiko `exec_command`；脚本显式读取系统代理配置并在同一 shell 中复测，退出码 0。
- 结果：
  - `/home/readme_to_codex.md`、`/home/readme_storage_to_codex.md`、`/home/readme_network_to_codex.md` 的 SHA256 分别为 `c9a0d2c282fbee03e4090144f1f858f1eed40c9411f28f4e7bb840e358236b0d`、`8d138594366b2bd2daef3ab8b03f4fb4b2f6f8195d4d67ca6f0b2cc2675b4e29`、`29a5bfa6f28db11695ec53dc9a898107ca229b5c10b74a3797b58a34b983c218`；与本地 2026-08-21 镜像一致。
  - `~/data -> /data/chenyiteng`、`~/shared -> /data/shared` 正确；`~/scratch` 不存在。源码、环境和小型缓存可放扩容后的 home，大资产、数据与运行结果放本人 data 目录。
  - Mihomo `1.19.30` 在 `127.0.0.1:7890` 监听；`/etc/profile.d/mihomo-proxy.sh` 给新 login shell 注入 HTTP(S) 与 SOCKS5H 代理变量。普通 Paramiko command shell不会自动继承，因此后续联网命令必须在同一个 command file 中显式 `source` 后复核。
  - 通过代理访问 Hugging Face 返回 HTTP 200；强制直连仍超时。通过代理执行 RoboTwin `git ls-remote` 成功，HEAD 仍为 `30954692d06ba7e89f7a6b76064f4062c488fa81`；RLinf π0 模型元数据也返回 HTTP 200。
  - `/etc/fstab` 已配置 `/home`、`/data` 的 `nodev,nosuid`，但当前 mount 输出尚未体现，符合“修改后尚未重启”的现场说明。此项不阻塞普通源码/数据工作，但任何依赖 suid/device node 的假设都不可成立。
- 网络风险：公共说明中的订阅快照显示总额 100 GiB、约剩 68.56 GiB，并标注 2026-08-23 到期；大下载前必须逐项核对远端体积，不能一次拉取整个 RoboTwin 1.38 TB 数据集。
